1. Nghiên cứu lại cơ chế giữ khoá giữa lúc cập nhật ở server và client gọi api (dùng cơ chế none-blocking ở server)
a, Dùng acquire
frame = None
if lock.acquire(blocking=False):
    try:
        if latest_frame is not None:
            frame = latest_frame.copy()
    finally:
        lock.release()
else:
    # Không lấy được lock, tránh chờ đợi
    return {"status": "busy"}
b, dev lại api chỗ các phương thức get/post chuyển về hàm async (bất đồng bộ) lẫn cả phần cập nhật frame trong backend

2. Nghiên cứu làm lại phần chat và video từ poll request liên tục thành giao thức websocket

